# Uso de esta carpeta

`AGENTS.md` debe indicar al agente cuándo consultar el índice de contexto y qué reglas globales aplicar.

Las skills disponibles están en `.agents/skills/`.

Cada skill puede remitir a archivos de `context/`, `casos/`, `decisions/`, `checklists/` y `plantillas/` cuando sean relevantes.
